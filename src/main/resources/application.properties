# This connection string allow you to connect to the database if it is in the same machine
spring.datasource.url=jdbc:mysql://localhost:3306/db_users_springboot
# This connection string is needed if the api is run in container mode and the database runs in the host machine
#spring.datasource.url=jdbc:mysql://host.docker.internal:3306/db_users_springboot

spring.datasource.username=root
spring.datasource.password=sasa
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.SQL=debug